@model string
@if (!string.IsNullOrEmpty(Model))
{
    var cls = Model.StartsWith("Error:", StringComparison.OrdinalIgnoreCase) ? "alert-danger" : "alert-success";
    <div class="alert @cls" role="alert">
        @Model
    </div>
}
